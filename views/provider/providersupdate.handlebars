<a href="/inventory">voltar</a>
<form class="form-createProducts" action="/providerupdate" method="POST">
<input type="hidden" name="id" value="{{provider.id}}">
    <h2>Cadastro de Fornecedores</h2>
    <div class="container-form">
        <div class="form-products">
            <label for="name">Nome</label>
            <input type="text" name="name" value="{{provider.name}}"required>
        </div>

        <div class="form-products">
            <label for="phone">Telefone</label>
            <input type="text" name="phone" value="{{provider.phone}}" required>
        </div>
        <div class="form-products">
            <label for="email" name="email">E-mail</label>
            <input type="email" name="email" value="{{provider.email}}"required>
        </div>
        <div class="form-products">
        <input type="submit"  value="Cadastrar">
        </div>
    </div>

</form>

